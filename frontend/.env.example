# Copy to .env.local for local dev (npm run dev picks it up automatically).
# When running via the root docker-compose.yml this is injected as a normal
# environment variable instead — see the root .env.example.

# Base URL of the gateway. The frontend never calls individual services
# directly (see architecture/api-contracts.md).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
